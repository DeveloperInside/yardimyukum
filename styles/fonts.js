import { StyleSheet } from 'react-native'

const fonts = StyleSheet.create({
  header: {
    fontSize: 20,
    fontWeight: '600'
  },
  button: {
    fontSize: 16,
    fontWeight: '600'
  }
})

export default fonts
