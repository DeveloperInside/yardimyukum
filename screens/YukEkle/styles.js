import { StyleSheet } from 'react-native'
import { sizes } from 'styles/commonStyles'

const styles = StyleSheet.create({
  fab: {
    position: 'absolute',
    alignSelf: 'center',
    bottom: sizes.maxi.md
  }
})

export default styles
